#ifndef HARVARD_REFERENCES_SERVER_TYPES_H
#define HARVARD_REFERENCES_SERVER_TYPES_H
#include <vector>
#include <string>

using StringArray = std::vector<std::string>;

#endif //HARVARD_REFERENCES_SERVER_TYPES_H
